# Supported Swift versions

This file tracks the Swift language versions that `swift-isolation-map` has a matching `IsolationRuleSet` for. The weekly `swift-version-watch` CI job compares the latest `apple/swift` GitHub release against the last line of this file and opens an issue when a new version needs review.

Last known Swift release checked against this tool's isolation rules:

6.3
